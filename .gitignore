__pycache__/
*.pyc

# Kファイルは再取得可能な中間物（生成物 motorUsage.json のみコミットする）
data/kfiles/*.lzh
data/kfiles/*.txt
data/kfiles/*.part

# 自動実行(dailyMotorUsage.ps1)のログ・ロック
scripts/logs/

# verifyDashboard exe生成物（バイナリはコミットしない）
dist/
build/
*.spec

# Kファイル成果物のローカル保管（commitしない）
localdata/
freshness_summary.md

# コース別直近10走（毎日変わる中間データ。使う直前に生成する）
data/courseLast10.json

# 検証ページ /kensho/ のキャッシュ（累積 data/kenshoVerify.json はコミット対象）
data/programsCache/

# Node（buildPlayersApp.mjs のビルド用。成果物 app.js のみコミットする）
node_modules/
package-lock.json
package.json
