# pageAuditFixPlan20260911b ─ 保留分の追加修正（自走型）

前の計画：`notes/pageAuditFixPlan20260911.md`／記録：`notes/pageAuditProgress.md`／台帳：`notes/pageAudit20260910.md`

## 0. 前回からの変更点（ここが本題）

前回は「触ってよいファイル」を HTML に限定したため、文面の正本が JSON や別スクリプトにあるものが保留になった。
今回は **対象の文字列で正本を特定する**。

- 各テーマの「対象文字列」を `git grep -n -F` でリポジトリ全体から探し、見つかったファイルと、そのファイルを生成しているスクリプトを「触ってよいファイル」とする
- ただし前回計画 第2節の「絶対禁止」に当たるファイルは除く
- 見つかったファイルが生成物なら、生成物ではなく生成スクリプト側を直して再生成する

関門・マージ後の再確認・差し戻し・`[updates]`・禁止・打ち切りは、**前回計画の第2節をそのまま適用する**（M1 の許可範囲だけ上の規則に置き換える）。
worktree は前回の `boatraceAuditWork` を使い、`origin/main` から新しいブランチを切る。本体の作業ツリーには触らない。

## 1. 裁定（2026-09-11 けん「最適化で進めさせろ」＝統括の推奨で確定）

| # | 項目 | 確定 |
|---|---|---|
| 1 | 検証01の3表の件数差・「386日」 | まず `results/` から再集計して元の表を再現する。再現できたら、表を固定値から再集計の出力に置き換え、見出しに対象条件を書く。再現できなければ、各表の見出しに「表ごとに対象の条件が異なる（元の集計条件は未記録）」と注記し、「386日」は期間の表記（2025/7/15〜2026/8/6）に置き換える |
| 2 | 占いの「複勝率」 | 値が3着以内の割合ではないことは確認済み。`racerStats.json` の `fukusho` を作っているスクリプトで中身を特定し、その意味どおりの呼び名に変える。意味を特定できなければ、その項目を表示から外す |
| 3 | 24場の注記にある出典（ひつじのボートレース分析・kyoutei-navi 等） | 変更しない（表示している風の方位・潮の説明の出典を明記している事実の記載のため） |

## 2. テーマ（この順に回す）

### U1 場の文化・24場（BGMと予想屋の文面）

| 項目 | 内容 |
|---|---|
| 台帳ID | P5・J7・N9（前回 T5 の保留分）＋24場カードの「緊張・高揚／締切BGMなし」 |
| 対象文字列 | `AI提供`・`memo保留`・`present=unknown`・`要確認`（予想屋の人数に関するもの）・`全国で唯一`・`70年級`・`日本一堅い`・`締切BGMなし` |
| 想定の正本 | `docs/data/yosouya.json`・`docs/data/fanfareBgm.json`（grep で確定させる） |

- `AI提供`・`memo保留`・`present=unknown` を含む文、および「AI提供情報で要確認」を含む文を削除する。確認できていない人数は数字を出さず「人数は未確認」とする
- 「全国で唯一“予想屋組合”を持つ」の「全国で唯一」を削る
- 「開場から70年級のベテランも珍しくない世界だが」を削る（前後の文がつながるように接続だけ直す）
- 「“日本一堅い”と言われる大村」→「“堅い”と言われる大村」
- 締切BGMが無い場の分類を「BGMなし」にする（`/fan/` は前回すでに対応済み。同じデータを使う24場カードにも出るようにする）
- M2 消えるべき語：`AI提供`・`memo保留`・`present=unknown`・`全国で唯一`・`70年級`・`日本一堅い` が読者向けの文字列で0件。24場の若松・芦屋カードで「緊張・高揚」が0件

### U2 見立て文の残り（あられ）

| 項目 | 内容 |
|---|---|
| 台帳ID | P3・J8 の残り（前回 T1 の保留分） |
| 対象文字列 | `カドの一撃`・`インの格が軽い` |
| 想定の正本 | `scripts/build_arare.py`（生成物 `arare.json`） |

- 「(カドの一撃)」「(インの格が軽い)」は括弧ごと削除する。同じファイル内の半角括弧は、読者に見える文字列だけ全角にする
- マージ後、`arare.json` を作るWFを dispatch し、生成物で M2（2語とも0件）を確認する

### U3 見どころ：逃げ時2着表の名前切れ

| 項目 | 内容 |
|---|---|
| 台帳ID | J10 の残り（前回 T2 の保留分） |
| 対象 | `docs/highlights/index.html` の逃げ時2着表で選手名を4文字に切り詰めている処理 |

- 出走表（前回 T2）と同じ方式で、選手名を切り詰めない（幅が足りなければ折り返す）
- M4：幅390px で見どころの逃げ時2着表を開き、6文字以上の選手名が欠けずに出ていること

### U4 用語辞典

| 項目 | 内容 |
|---|---|
| 台帳ID | J5・J6（前回 T10 の保留分） |
| 対象文字列 | `きこう`・`主に1・2コース`・`内側に固まった艇の一番外`・`深イン` |
| 想定の正本 | `docs/data/kansenki/glossary.json` |

- 帰郷の読みを「ききょう」にする
- カド＝ダッシュ勢の最も内側（多くは4コース）
- スロー勢＝主に1〜3コース、ダッシュ勢＝主に4〜6コース（カド受け＝スロー勢の最も外側＝多くは3コース、と整合させる）
- 深インは、公式サイト（boatrace.jp）等の一次情報で定義を確認できた場合だけ直す。確認できなければ触らず記録する
- `glossary.json` を観戦記など他の表示でも使っている場合は、その表示も同じ定義になることを確認する
- M2：`きこう`（帰郷の読みとして）0件、`主に1・2コース` 0件、`内側に固まった艇の一番外` 0件

### U5 検証01（勝負駆け）

| 項目 | 内容 |
|---|---|
| 台帳ID | N4 の残り（前回 T7 の保留分） |
| 想定の正本 | `scripts/templateKensho.html`・`scripts/buildKensho.py`（再集計を足す場合） |
| 再生成 | `updateKensho.yml` を dispatch（JST 11:00〜11:40 を避ける） |

- 第1節の裁定1のとおり。再集計を試みる場合、元の表（帯表の和33,027、枠別表の和7,173、チルト表の崖っぷち7,254）と各値が一致するかを数える。**1件でも合わなければ「再現できない」とし、注記の方に進む**
- 再現できた場合も、`predictions/`・`verify_log.csv` には触れない

### U6 占い

| 項目 | 内容 |
|---|---|
| 台帳ID | J14（前回 T12 の保留分） |
| 対象文字列 | `複勝率`・`fukusho` |
| 想定の正本 | `docs/uranai/index.html`・`racerStats.json` を作るスクリプト（grep で特定） |

- 第1節の裁定2のとおり。`fukusho` を作るコードで計算内容を特定し、呼び名を変える。特定できなければ、その項目を表示から外す
- `racerStats.json` の値そのものは変えない（呼び名だけ、または表示から外すだけ）

## 3. 終了時

1. `notes/pageAuditProgress.md` に U1〜U6 の行を追記する（各テーマのPRに同梱）
2. `notes/state.md` の「裁定待ち／全ページ監査の保留分」を実施結果で更新し、第1節の3行を裁定ログに追記するPRを1本
3. けんへ返すのは次の1行だけ

```
完了 / 実施テーマ数 / 保留（番号と一言） / 差し戻し（番号） / 最終 main SHA
```
