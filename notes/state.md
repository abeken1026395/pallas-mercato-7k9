# state — 進行状況

このファイルは運用の記録であり、読者向けの公開ページではない。
毎回のチャット終了時に更新する。事実・仕様は `notes/facts.md`、行動規範はプロジェクト指示（L1）にある。

最終更新 2026-09-05 JST ／ 基準 main `593557b5`

---

## 進行中

|テーマ|状態|次の一手|
|---|---|---|
|プロンプト・引き継ぎ体制の再構成|`notes/` 新設中|本ファイルと `facts.md` を投入 → メモリ整理 → L1貼り替え|
|けんの実オッズ記録|2026-09-04 住之江から開始|レース / z / 買ったか見送ったか / 締切直前オッズ / 結果。**見送りも1行書く**。n=20〜30で帯ごとの妙味を判定|

## 未マージPR・作業ブランチ

なし。

---

## 期限あり

|期限|内容|
|---|---|
|**9月上旬**|`motorParts` 4日分の埋め戻し（20260812 / 0813 / 0814 / 0817・計4,307行）。材料は `preview/*.json` に全日存在。**復元→`backfillMotorPartsMotorNo.py`→`buildMotorMaintenance.py` 再ビルド**が必須工程。`anteiban` のみ復元不可。識別は `取得日時` が空の行。**ローカルCode限定**|
|**2026-10-31**|短縮秒の再検定（直近3ヶ月窓）。上の埋め戻しが前提|
|**10月下旬**|`haranModel` の再測（walk-forward・**最低2ヶ月**）。上位10%の①着外率が40%を割ったら係数を再学習|

## 裁定待ち

- `racerInRate.json` の更新WF新設可否。新設するなら週1回・JST深夜0:00〜4:00・専用concurrency、かつ**「出典」キーを生成スクリプト側で出力してから**作る
- コース別直近10走の表示。**案A＝足す / 案B＝置き換える**が未決。`/next/` で試作
- `claude/syntax-id` の取り込み可否（`build_highlights.py` に13行・**文面が変わる**）
- `docs/aisho-suminoe/`・`docs/shobuun-suminoe/` の削除（けんが可と明言・未実施）
- `scripts/template_racers.html` の navtab から上記2ページへのリンク。`/uranai/` に修正するかリンク削除か
- 検証シリーズの次テーマ選定（期の級別ボーダー／残走数と到達可能得点率／賞金ランキング）。**選定基準は「俗説が実際に口にされているか」を先に確認**

## 別チャット送り（着手しない・報告のみ）

- **C-1 被まくり艇の検証**。A/B/C 3群（Aまくられた / B他の負け方 / C3着内）。**A vs B が仮説の本体**。次走の進入コースを統制する。位置づけは新特徴でなく `last20` のノイズ除去
- **C-2 機力低×展示速**。「①のモーター2連率が場平均を下回るとき、展示が6艇1位でも①着外率は下がらない」。**2×2で集計・条件を後から足さない**。使える期間は2026-04以降の約5ヶ月（`motorUsage.json` の制約）・想定n=1,800
- 一周・まわり足のデータ源調査。`preview` に項目なし・過去分も遡れない。場公式（尼崎・桐生）の取得可否は**不明**
- **WFの失敗通知が無い**。9便の failure が4日間気づかれなかった
- リモートブランチの仕分け。**総数34本**・保護6本・削除待ち22本・メモ未記載の残余あり。判定は**merge-base の3点比較**（`git diff --name-only` も `git branch --merged` も使えない）。削除はローカルCodeかGitHub画面（クラウドは403）
- Stop hook の条件修正
- 選手コメントの調査（公式に存在しない。所在・取得可否・著作権・カバー率）
- `lintGuard.py` の検査軸変更（判定式リテラル依存をやめ、L2の7ページも canonical 照合へ）
- SRI（Subresource Integrity）の導入検討
- ホスティング移行（さくら14日無料お試し → 本契約 → Private化判断）

## 小さい残件

- コピーガード未実装3件：`motor-maintenance` / `kensho/shobugake` / `kensho/taiju`（すべて生成物・正本を直す2段階）
- `docs/data/motorParts.json`（15.68MB）・`docs/data/kansenki`（10.7MB）の削減
- `buildRacerStatsSplit.py` に `--check` モード追加
- 決まり手エンドポイント特定（PCのF12 Networkで日和の `.php` を確認）
- 潮位データ（気象庁天文潮位）の本格組込
- 蒲郡(07)・芦屋(21)の水質を公式で裏取り
- オッズの表示設計 ／ 過去分の遡及（表示設計が決まってから）
- 心理レイヤーUI（損失回避・追い上げ・サンクコスト警告・出所明記）
- 動的データ深層（今節モーター2連率偏差・節経過日数・企画レース当日判定・当日干満）
- `/next/` でのリニューアル版試作
- NEWバッジの削除（`docs/index.html` のバッジ用scriptブロックごと）
- `20260727` のモーターNo空件数369件が突出している理由
- `/motor-maintenance/` のiPhone12列表示が未検証／節内日7〜9日目が小分母（n=40〜43）
- トップの `c-motor` の罫線色が2枚同じ（軽微）
- `updateResultsLive.yml` 4行目のコメントに旧リポ名 `BoatraceOpenAPI` が残存（動作に影響なし）
- 共通ナビ13px・コピーライト10px の文字サイズ
- `/payouts/` の当日反映（優先度低）

---

## 裁定ログ

|日付|決めたこと|適用範囲|
|---|---|---|
|2026-09-05|残タスクの通番を廃止。期限あり／裁定待ち／別チャット送りの3区分へ|`notes/state.md`|
|2026-09-05|`notes/` を新設。`analysis/` は検証レポート専用として現状維持|リポジトリ構成|
|2026-09-04|スタート展示Fのルールを「1着のみ禁止」に変更（2着は解禁）。①は単独でFを理由に除外しない例外は維持|けん個人の予想|
|2026-09-04|ホールドバンド（z −1.03〜+1.05）は買わない|けん個人の予想|
|2026-08-28|`haranModel` 再学習の判断を10月下旬へ前倒し（バックテストで測れるため）|モデル|
|2026-08-26|買い目の線引きを変更。**けん本人へのチャット内回答では出す**／サイトは従来どおり出さない／`predictions`・`verify_log` は汚さない|全体|
|2026-08-23|閾値確定：機力差30pt（PR #273）・展示0.03秒（PR #274）。**もう動かさない**|見どころ|
|2026-08-21|`mitateJissoTable.md` の大原則「数字は貼らない」を**撤回**|見どころ|
|2026-08-20以降|見どころ改稿は更新履歴に載せない（掲載は PR #265 の1件のみ）|`[updates]`|
|2026-08-19|コピーガードは canonical 照合方式へ。URL変更時の修正不要に|8ページ|
|2026-08-04|`docs/aisho-suminoe/`・`docs/shobuun-suminoe/` は触らない・削除可|サイト|
|2026-07-08|検証ビューワー（`/verify/` ページ）は作らない|サイト|

---

## 現場実測メモ

### 2026-09-04 住之江

けんが現地オペレーター兼カメラマン。ワークフローは**2枚のスクリーンショット（出走前情報＋スタート展示）＋API自動取得（登番・級別）**。
記録項目：レース / z値 / 購入orホールド / 締切前オッズ（目視のみ） / 結果（ホールドも記録） / **スタート展示Fが出た枠とその着順**。

9/3の12Rリハーサルでホールドバンドを破って購入し損失（3-1-6・5,750円）。

### 2026-08-28 尼崎（n=5・観察であって実証ではない）

- 5R 5-1-3 / 7R 5-6-2 / 11R 1-3-2 / 12R 5-1-6
- 統括は5レースすべてで⑤を「相手」に置き、**頭には置かなかった**。尼崎の5コース1着率は5.3%（n=1,945）。**5回中2回頭に来た事実**は記録する
- **最優先1：機力の劣る①を、展示の足を理由に頭へ固定してはいけない**。準優3本すべてで捲られた（砂長41.05・山崎25.88・木下45.26）。**展示は単独走で、捲られる場面が存在しない**
- 前走の着順は「進路を潰された結果か」で意味が反転する。⑤安河内は前走1コースで沈み、次走5コースで1着

---

## 未判定・保留

- `results/*.json` の2025年分に級別キーが無い（**2026-01-01でスキーマが変わる**）。`rankHistory.json` から復元する実装が `backtestHaran2025.py` にある
- A-2 の AUC −0.0171 は要因4つが分離不能で「安定と確認できた」とは書けない
- 展示行の出現率57.1%（8/23・168レース）は多い水準。**数日ぶん観測してから再判断**
- `collapsePattern` / `racerInRate` / `courseFinish` は生成スクリプトを手動実行すると「出典」キーが消える。**次に再生成するとき生成スクリプト側へ同時反映**
- 丸め桁のフィンガープリントは元データからの再計算が要るため未実施。**次に生成スクリプトを回すときに同時に仕込む**
