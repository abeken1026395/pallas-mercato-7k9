# facts — 事実台帳

このファイルは運用の記録であり、読者向けの公開ページではない。
確定した仕様・数値・構造だけを置く。進行状況は `notes/state.md`、行動規範はプロジェクト指示（L1）にある。
更新は月1回程度。**確定した事実だけを書き、推測を書かない。**

最終更新 2026-09-05 JST ／ 基準 main `87491dad`

---

## 1. 環境

- GitHub: **abeken1026395/pallas-mercato-7k9**（Public・Pages公開は `/docs`）
- 公開URL: https://abeken1026395.github.io/pallas-mercato-7k9/
- 改名履歴: boatrace → lumen-vortax-4z2 → pallas-mercato-7k9（2026-07-26）
- **旧名はリダイレクトしない。** lumen-vortax-4z2 は別リポジトリとして意図的に占有し「おかえり」ページを設置済み。旧remoteを持つCode環境は404にならず空リポを正常にfetchし「履歴が消えた」と誤診断する
- iPhone主体。WindowsPC（Python 3.14 MS Store版）＋Claude Code。クラウドCodeも併用
- ルート階層（12）: `.github` `analysis` `data` `docs` `health` `kdata` `odds` `predictions` `preview` `pv2` `results` `scripts`
- `analysis/` は検証レポート専用。`notes/` は運用ノート専用

### 到達可否

|対象|アシスタントbash|クラウドCode|ローカルCode|
|---|---|---|---|
|`raw.githubusercontent.com` / `api.github.com` / `github.com`|**可**|可|可|
|`github.io`|不可|—|—|
|`boatrace.jp`|不可|未確認|**可**|
|`mbrace.or.jp`|不可|**不可**（IPブロック）|**可**|

- `api.github.com` は**未認証だとレート制限に当たる**（403）。そのときは `github.com/<repo>/commits/main.atom` から最新SHAを取り、`raw` をSHA固定で叩く
- アシスタント環境には **Playwright（Chromium）** がある。ローカルHTTPサーバを立てて実表示を検証できる

### クラウドCodeとローカルCodeの使い分け

|作業|クラウド|ローカル|
|---|---|---|
|読み取り・編集・PR作成・squash merge・WF手動実行|**可**|可|
|ブランチ削除（`git push origin --delete`）|**403で不可**|**可**|
|`boatrace.jp` / `mbrace` 取得・観戦記自走・`courseFinish` 再生成|不可|**可**|

- **Cowork（デスクトップアプリ）は push できない。**リポジトリを触る作業はClaude Code側で回す
- **Google Driveの既存ファイルは編集できない**（読み取り・新規作成・コピーのみ）。だから進行状況は `notes/` に置く
- クラウドCodeに `gh` CLI が無い環境がある。その場合は GitHub MCP でPR作成・マージ
- クラウドCodeのクローンは **shallow** のことがある。作業ブランチは `git checkout -B <name> origin/main` で直接切る
- クラウドCodeのPR本文には環境規約で生成元表記が自動付与される。読者に出ないため放置でよい

---

## 2. サイト構成

トップからリンクされている公開ページ（15）

`/` `/racers/` `/highlights/` `/motor/` `/motor-maintenance/` `/players/` `/stadium/` `/results/` `/payouts/` `/fan/` `/uranai/` `/announcers/` `/glossary/` `/updates/` `/kensho/`

トップから直接リンクしていないページ

- `/<場名>-payouts/` … 24場それぞれのR別万舟率（`/payouts/` から遷移）
- `/aisho-suminoe/` `/shobuun-suminoe/` … **触らない**（2026-06-27住之江女子戦の遺物・`SEED=20260627` ハードコード）
- `/next/` … 実験場
- `/probe/beforeinfoProbe.html` … 内部確認用（生成物）

### 導線

全ページの `<body>` 直後に同一マークアップ。CSSは各ページにインライン。

- `.siteback` … 全ページ共通のトップ戻り（min-height 44px・color:inherit）
- `.sitelink` … 横移動
- `#root` を持つページでは必ず `#root` より前に置く
- **行き止まりページはゼロ**
- quicknav は内部14件＋YouTube＝**15タイル**。ラベル4文字以内（`.qn-lb` 9px・幅56px固定）。**16件目でレイアウトが4行になる**（裁定が要る）
- 現在地表示は見送りで確定

### 検索エンジン遮断は意図的（削除禁止）

`docs/robots.txt` は `User-agent: * / Disallow: /`、全HTMLに `noindex, nofollow`。
オープンチャット参加者だけが見られる状態を維持するため。**事故ではない。**

---

## 3. 正本と生成物（絶対に守る）

生成物を直接編集すると次回の自動実行で消える。**復元は `git checkout --` では不可。正本から再生成する。**

|生成物|正本|再生成|
|---|---|---|
|`docs/racers/index.html`|`scripts/template_racers.html`|`scrape_racers.py`（update_racers.yml）|
|`docs/motor/index.html`|`scripts/template.html`|`scrape_motors.py`（update_motors.yml）|
|`docs/motor/app.js`|`scripts/motor/app.jsx`|`node scripts/buildMotorApp.mjs`（**Babel 7系を明示**）|
|`docs/motor-maintenance/index.html`|`scripts/templateMaintenance.html`|`buildMotorMaintenance.py`|
|`docs/players/app.js`|`scripts/players/app.jsx`|`node scripts/buildPlayersApp.mjs`（**Babel `@babel/core@^7` 必須**）|
|`docs/results/data/*.json`|`results/*.json`|`buildResultsSite.py`（**直近30日のみ**）|
|`docs/kensho/shobugake/index.html`|`scripts/templateKensho.html`|`buildKensho.py`（updateKensho.yml・JST 11:20）|
|`docs/kensho/taiju/index.html`|`scripts/templateTaiju.html`|`buildTaiju.py`（同WF）|
|`docs/data/venueMeta.json`|—|`scrape_racers.py`（毎日）|
|`docs/data/collapsePattern.json`|—|`buildCollapsePattern.py`（**WFなし・手動のみ**）|
|`docs/data/racerInRate.json`|—|`buildRacerInRate.py`（**WFなし・手動のみ**）|
|`docs/data/courseFinish.json`|—|`buildCourseFinish.py`（**kdata由来・クラウド再生成不可**）|

**`/motor/` は2系統が同居する。**`docs/motor/index.html`（`scrape_motors.py` 生成）と `docs/motor/app.js`（`app.jsx` 由来）は別系統。片方を直すときもう片方を触らない。
`scripts/motor/app.jsx` 末尾の「レイアウト計量テーブル」以降39行は著作権表示のガード。**差分ゼロを保つ。**

上記以外の `docs/*/index.html` はすべて手書き。**トップ・`/results/`・`/kensho/` ハブ・`/highlights/` も手書きの一次ソース。**
`docs/players/profile.json` は手入力。**Actionsの対象に絶対しない。**
払戻24ページ・相性・勝負運の26ファイルはテンプレを持たない手書きの複製。1本直しても波及しない。

---

## 4. データ構造

### 選手

- `docs/data/racerStats.json`：1,643名・期首固定の**正本**（唯一の供給元）→ `racerStatsCore.json`（11キー・初期fetch）＋ `racerStatsDetail.json`（17キー・遅延fetch）
- `racerStatsCore.json` に**性別フラグ `female`** がある。**最低体重が男子52.0kg・女子47.0kgと違うため、体重を扱う集計では必ず分ける**
- `docs/highlights/racerStatsToday.json`：見どころ登場選手の和集合892名・`build_highlights.py` が毎朝生成
- `docs/data/rankHistory.json`：現役1,643名の級別推移（差分形式・367KB・遅延fetch）。日付は改定日ではなく**改定後の初出走日**
- fanファイル命名：fan2510＝2026前期 / **fan2604＝2026後期（現行最新）**。fan2607は存在しない。次回は fan2610（12月頃）
- 期別成績は期の途中で変わらない。出走表CSVの勝率との食い違いは「古い」のではなく別指標

### レース結果・直前情報

|ファイル|中身|
|---|---|
|`results/*.json`（**106MB / 409日・絶対に消さない**）|着順・決まり手・全式別払戻・艇別・実測条件7項目。2025-07-15〜|
|`preview/*.json`|展示タイム・体重・調整重量・チルト・展示ST・展示進入。2026-01-01〜|
|`odds/*.json`|7式別。2026-08-04〜前向き蓄積|
|`docs/data/courseFinish.json`|327,575件の入着分布|
|`docs/data/stadiumBearing.json`|24場の水面方位（2M→1Mの方位角・真北0°・時計回り）|

- **`results` の `コース` が本番の進入コース（こちらが正）。`preview` の `racer_course_number` はスタート展示の進入。**両者は日によって2〜8%食い違う
- 型が違う：`preview` は `stadium_number`・`race_number` が **int**、`results` は `場コード`="01" 文字列・`レース`="1R" 文字列。**突合には型変換が必要**
- `weight_adjustment` は**0.5kg刻みのみ**（223日で非0.5刻みゼロ）
- **枠番と実際の進入コースは7.75%ずれる**（桐生12.5% / 江戸川0.8%＝15倍差）。直近N走は**進入コース別**で取る
- 展示タイムの充填率は全体98.38%（津91.6% / 江戸川93.5%）
- **`preview` に周回展示・まわり足・直線に相当するキーは存在しない**（2026-08-28実物確認）。後継APIが返しておらず過去分も遡れない

### ★`results/*.json` のスキーマは 2026-01-01 で変わる

|日付|級別キーを持つ艇|
|---|---|
|2025-12-31|**0 / 1,152**|
|2026-01-01|**930 / 930**|

2025年分の艇別項目は `枠・登番・氏名・コース・ST・着` のみ。取得元が違う（2025＝旧v2 / 2026＝後継v1）。
2025年分の級別は `rankHistory.json` の「当日以前の最後のエントリ」で引く（`backtestHaran2025.py` に実装済み・3,594艇中一致3,589）。

**→ 「`results/` は409日ぶんある」は全項目に成り立たない。特徴ごとに使える期間を確認する。**

### 確定した対応表

|対象|対応|
|---|---|
|風向コード|1=北 〜 16=北北西（22.5°刻み・時計回り）・**17=無風**|
|天候コード|1=晴 / 2=曇 / 3=雨 / 4=雪 / 6=台風（5は未出現）|
|**着**|7=妨害 / 8=エンスト / 9=転覆 / 10=落水 / 11=沈没 / 12=不完走 / 13=失格 / 14=F / 15=L / 16=欠場 / 99=`_`|
|単位|風速=m / 波高=cm / 気温・水温=℃ / 体重・調整重量=kg|
|グレード|1=SG / 2=G1 / 3=G2 / 4=G3 / 5=一般|
|最低体重|男子52.0kg / 女子47.0kg|

- **★平均着を出すときは 7〜15 を6に丸め、16 は分母から除く。**生の `int()` だと転覆が「9着」として平均に入り、実測で最大0.15ずれた。1着率・3着内率・①着外率は `==1` / `<=3` / `>=4` の判定なので影響しない
- **風向は「吹いていく方位」。** `weather.json`（Open-Meteo）の `deg` は「吹いてくる方位」で**定義が逆**。直接比較すると180°ずれる
- `/results/` の風判定：風向角＝(コード−1)×22.5、水面軸との角度差45°以下＝追い風・135°以上＝向かい風・それ以外＝横風。全58,018レースで横風49.3% / 向かい風23.1% / 追い風21.4% / 無風6.2%

---

## 5. データ源とワークフロー

|系統|データ源|WF|窓|
|---|---|---|---|
|`results/`|boatraceopenapi/api v1|`updateResultsLive.yml`（毎時 UTC `15 2-14`）＋`updateResults.yml`|`RESULTS_WINDOW=1`|
|`preview/`|boatraceopenapi/api v1|`updatePreview.yml`（1日16回）|`PREVIEW_WINDOW=2`|
|`odds/`|turnmark/api v1|`updateOdds.yml`（JST 07:00・13:00）|`ODDS_WINDOW=2`|

取得パスは `raw.githubusercontent.com/<repo>/gh-pages/docs/v1/YYYY/YYYYMMDD.json`。

- 後継API（boatraceopenapi・turnmark）は**2026-01-01以降のみ**。旧 BoatraceOpenAPI v2 は非推奨だが停止しておらず、**2025年分はそこからしか取れない**
- **turnmark は当日ファイルを出さない**（前日分を翌朝JST 05:34頃に1コミット）。一本化は不可
- **登番・級別は boatraceopenapi から自動取得できる**（登番＝`racers[frame].number`／級別＝`rank_number_source` の文字列値）。**APIの `rank_number` は A1=1…B2=4 で haranModel の `rk1`（A1=4…B2=1）と逆転する**
- 展示タイム・コース・スタートタイミングは **null**（現場ではスクリーンショットが必須）
- 気象：`fetch_weather.py` → `docs/data/weather.json`（24場×48時間・Open-Meteo・**予報値**）／`liveWeather.json` は boatrace.jp beforeinfo 由来の**実測**スナップショット。別物
- mbrace：`http://www1.mbrace.or.jp/od2/K/YYYYMM/kYYMMDD.lzh`（Kファイル・1日1ファイル・全24場）／`/od2/B/{YYYYMM}/b{YYMMDD}.lzh`（Bファイル・1996-07-19〜）。**ActionsからはIPブロック。ローカル一択**
- 公式 beforeinfo は**当日・前日のみ**。2日以上前はサイレントリダイレクト＝**取り逃せば永久欠測**
- ボートレース日和はJS動的描画で requests 不可。公式進入率は `/owpc/pc/data/racersearch/course?toban=登番` で取得可

### ローカルタスク4本

|タスク|時刻|内容|
|---|---|---|
|`boatrace-writeKansenkiLocal`|5:30|観戦記の自走|
|`boatrace-dailyMotorUsage`|6:00|Kファイル収集→motorUsage再生成→補填。**稼働中**（2026-09-05 06:05 更新を実測）。ログは `scripts/logs/dailyMotorUsage_YYYYMMDD.log` の3行だけ読む|
|`boatrace-dailyPartsBackfill`|**9:00 と 18:30**|モーターNo補填（本命は9:00）|
|`boatrace-updateKimarite`|毎月2・16日 6:30|決まり手更新|

**ps1はUTF-8 BOM必須**（BOM無しはPowerShell 5.1がcp932と誤読）。タスクは Interactive＋WakeToRun。

### Pages デプロイ（要点）

- **デプロイは同時1本・所要10分以上・10分でタイムアウト**。主因は容量ではなく**頻度**
- 各WFに `concurrency` を入れる。**グループ名はWFごとの専用グループ**、`cancel-in-progress: false`（例外は `docs/` にコミットしない `gitleaksScan.yml` のみ）
- **全WFを1グループに束ねてはいけない**：待機できるのは1本だけで、3本目が来ると待機中がキャンセルされコミットが静かに失われる
- マージ直後のデプロイが `cancelled` になり後続が `success` で終わるのは**標準挙動**（`updateUpdates.yml` が20〜30秒後に push するため）。**Re-run しない**
- **JST 11:00〜11:40 は `updateKensho.yml` と競合するのでマージ禁止**
- `docs/` 容量：2026-08-06に 99.9MB → **41.5MB / 784ファイル**（`SITE_DAYS = 30` と `prune()`）
- **GitHub Pages ではHTTPヘッダを設定できない**＝iframe禁止・ホットリンク禁止・Basic認証・アクセスログのいずれも不可

---

## 6. 検証済みの知見

### 荒れ指数モデル `data/haranModel.json`（PR #307・main `f4718023`）

6,403B ／ sha256 `21cd53232ca78f8d90e1d727b654b9926b07bfb29dee39308cdf0646c6d01168`
**リポジトリ直下に置く**（`docs/` に置くとPagesデプロイが起きる）。**切片は両方とも `null`＝順位付け専用。確率として扱わない。**

`morning7`（展示前・`build_highlights.py` の `HARAN_IDX` と同一・AUC 0.7108）

|キー|説明|係数|欠損代替|平均|SD|
|---|---|---|---|---|---|
|`rk1`|①の級別（A1=4..B2=1）|−0.33361|3.0|2.8736|0.8868|
|`last20`|①の直近20走平均着|+0.31777|3.3|3.3339|0.6364|
|`rkout`|外3艇の級別合計|+0.27857|7.1429|7.6915|1.8964|
|`venOut`|場の①着外率(%)|+0.18006|19.0486|19.4346|3.5752|
|`c1last10`|①の1コース直近10走平均着|+0.15208|1.9429|2.0853|0.7039|
|`avgSt`|①の過去平均ST|+0.14331|0.1601|0.1609|0.0212|
|`midLastBest`|②③の直近20走のよい方|−0.13212|3.1|3.1229|0.5298|

`afterTenji10`（展示後・けん個人の予想用・AUC 0.7214）＝ `morning7` に `tenjiDev1`（+0.21225）・`maedukeN`（+0.16181）・`tenjiSt1`（+0.03845）を加えた10特徴。**他の係数も引き直されている。**

標準化：`z = Σ 係数 × ((値 − 平均) ÷ 標準偏差)`。欠損は欠損代替値で埋める。

- **朝（展示前）とけんの予想（展示後）で同じ係数を使ってはいけない。**利用時点が違う
- **`tenjiSt1` +0.03845 は10特徴中で最小。展示ST単独で艇を消す根拠にならない**
- **②〜⑥艇の展示偏差は係数0.01未満で全部落ちている。**生き残ったのは①の展示偏差だけ＝「①の展示が遅ければ①は飛びやすい」であって「展示が速い艇は来る」ではない。**方向が片側だけ**
- **`motor1` はどちらのモデルにも入っていない。**`motorHistory.json` が節完結スナップショットのため過去分の復元は原理的に不可
- 住之江（venue code 12）の `venOut` 固定値は **18.03%（n=2,634）**

### A-3 帯別配当分布（PR #310・48,931レース・2025-10-01〜2026-08-22）

|帯|z下限|z上限|①1着率|①着外率|配当中央値|10000円↑|
|---|---|---|---|---|---|---|
|1|−2.414|−1.025|77.6%|**6.1%**|1,360円|12.2%|
|**10**|**1.052**|3.316|**25.5%**|**43.8%**|3,970円|20.0%|
|全体|—|—|55.1%|18.9%|2,480円|16.5%|

- **これは回収率ではない。**`odds/` は一切参照していない
- **上位帯は「万舟の帯」ではなく「中位配当の帯」。**①着外率は7.2倍になるが万舟率は1.6倍しか動かない
- **帯10でも②③頭が45.1%、外枠（④⑤⑥）合計は29.4%どまり**
- 決まり手：帯10で伸びるのは**まくり 7.3%→30.6%・差し 5.4%→19.0%**。恵まれは帯によらず1%未満
- 場別・グレード別の分解は**やらない**（1セル約200件・誤差±7pt）

けん個人の予想での使い方

|z|買い方|
|---|---|
|**z ≥ 1.05**|**①を頭にしない。**②③頭が45%|
|**z ≤ −1.03**|①頭78%・逃げ75%。ヒモ荒れのみ|
|中間|**見送り（ホールド）**|

**配当分布は分かったが、その配当を取れる買い目がいくらだったかは不明。**`odds/` は締切前1点で172レース中4件（2.3%）が確定払戻と20%以上ずれる。**回収率のバックテストは原理的にできない。**

### A-2 2025年後半バックテスト（PR #309）

AUC 0.6937（2026実測比 −0.0171）／上位10%①着外率 41.1%（**再学習トリガー40%割れせず**）。
**「安定と確認できた」とは書けない**：係数の経年劣化・学習期間への隣接・年固有変動・級別復元の精度差の4要因が分離できない。**逆走である以上、上がっても下がっても解釈できない。**

### 効いたもの・効かなかったもの

- 最大の改善は目的変数の修正：「荒れ（三連単配当≥5000）」→「**①が着外（4着以下）か**」で AUC 0.605 → 0.726
- 効き順：`cWin1` → `tenjiDev1` → `motor1`（M8時点）
- 展示STは AUC を 0.611→0.679 に上げる最強変数だが**本番STはリーク**。展示ST（`tenjiDev1`）が合法的な代替
- **枠なり崩れが起きたレースは荒れる方向に効く**（係数+0.155・重要度7位）。ただし単独では弱い
- **効かないと確定済み（再挑戦不要）**：天候・風速・レース格・ボート番号・場別モデル・`vuln1` 減点方式・展示×機力の交互作用・勝負駆け・体重（節内変動）・`cWin1` 単体（`last20` があれば不要）

### 検証01 勝負駆け（結論：効かない・負の発見）

- **表記は「勝負駆け」。**「勝負掛け」「勝負賭け」は誤り
- 公式 `pointrank` は G2以上のみ。**G3/一般＝92.7%（700/755節）が空白領域**
- 成立条件：統制後の係数が正（+0.453）／チルト3帯の最大差1.0pt以内（0.3pt）／1枠−6枠が35pt以上（44.1pt）

### 検証02 体重（2026-08-17公開）

俗説「体重を絞ってきた選手は本気」→ **答えは「いつ絞っているのかが見つからない」。**

|項目|値|
|---|---|
|節内カーブ（男）|2日目 −0.17 → 6日目 −0.15kg（**2日目で止まる**）|
|節内回帰（着順点/kg）|+0.119（**符号が逆**・実効0.03点）|
|節内体重偏差SD|**0.28kg**（錘の最小刻み0.5kgより小さい）|
|選手ごとの体重（男・中央値）|52.35kg。**最頻は52.0kgちょうどが367人**|
|錘あり率|男11.6% / 女40.3%|

成立条件5本を毎日自動判定。**walkaway：絞ってきた、は嘘ではない。終わったのがずっと前だっただけ。**

### モーターの新替と走行数（`motorUsage.json`）

**モーターは各場で年1回新替され、機番が振り直される。**通算の走行数は「同じ機番を名乗った歴代モーターの合計」であって、今走っている機の実績ではない。
実測：若松M49は10年通算で走2279・2連率36.4%。新替以降で数え直すと**走135・2連率51.1%**（公式のモーター2連対率53.7%とほぼ一致）。1サイクルの走行数は**115〜198走**。

新替日は**公式非公開**。「公式のモーター2連対率＝新替からの累計」という性質を使い、**自前の2連/走が公式レートに最も合う開始日を場ごとに総当たりで逆算**している。誤差1.5pt超・照合15機未満の場は推定失敗として**走行数を出さない**（推測で埋めない）。

- `docs/data/motorUsage.json` は `schema: "venueWindow-1"`。**場ごとの窓は `venues.<場コード>.coverageFrom`**（`fitError`・`matched`・`officialAsOf` を併記）
- **トップレベルの `coverageFrom: 20161101` は全体窓であって走行数の窓ではない。**ここを窓と読むと10年通算に戻る
- 2026-09-05実測：`venues` 22場・`motors` 1,389機。未推定は **02戸田・07蒲郡**の2場。新替日は場ごとに時期が違う（例 01桐生 2025-12-27 / 04平和島 2026-06-18）
- `buildMotorUsage.py` の安全ゲート（記号着順0.5〜5.0% ／ 走行数中央値100〜250）。NGの日は書かず `exit 2` で止まり、コミットもpushも後段のbackfillも走らない（前日のJSONが据え置き）。**合格域は緩めない**
- Kファイルを `data/kfiles/` から読む。**Kアーカイブはローカルにしかなくクラウドでは再生成不可**

### モーター整備

- **ランキングにしない（確定）。**主語は「節×機番」、選手は属性。**「この選手が替えた」と書かない**
- 節の**2日目**がもっとも手が入る（10.3%）。最終日はほぼ入らない（4.6%）。**準優前に手が入る形は出ていない**（負の発見）
- WF `updateMotorMaintenance.yml` は JST 10:00＋手動。**pushトリガは不採用**

### コース別直近10走

全体中央値：1コース2.1 / 2コース3.2 / 3コース3.3 / 4コース3.6 / 5コース4.0 / 6コース4.5着。
**当地は扱わない**（当地×1コースで10走以上は1.6%＝分母の見えない率になる）。

### 選手×進入コース別の入着分布 `racerCourseStats.json`（2026-09-05・PR #347〜#352）

- 生成は `scripts/buildRacerCourseStats.py`（入力 `results/*.json`）。**手動生成のみでWFに載っていない**。期間は 20250715-20260905 で固定
- 保持は**回数だけ** `[n, c1, c2, c3, c4plus, dq]`。率は表示側で計算する（JSONに丸めた率を持たない）
- 区分は `courseFinish.json` と揃えた。**dq（着7〜15）は分母に含める**。欠場（着16・663件）と進入コース不明（12件）はその1走だけ分母から除き、同じレースの他艇は残す
- 実測（63,145レース・378,195走・1,648名）

|進入|全体1着率|2連対内|3連対内|選手あたりN中央値|
|---|---|---|---|---|
|1|55.1%|72.2%|81.2%|37|
|2|13.2%|38.3%|57.2%|41|
|6|2.0%|8.3%|22.2%|35|

- Kファイル由来の `courseFinish.json`（別期間）の1コース55.3%と0.2pt差。定義が揃っていることの裏取り
- **期間は全期間1本のみ。直近6ヶ月は率に使えない**：180日窓では20走以上のセルが32.2%しかなく、選手の28%（465名）が1コースも表示できない（`racerStartOrder.json` から実測）。平均STのような連続値なら2期間持てるが、二項の率では母数が足りない
- **1着率に小数第1位を出さない**。1コースは中央値37走で、1着1本が2.7pt動く（SE ±8.2pt）。fan由来の「52.4%」は過剰精度だった
- 母数ガードは20。未満は率を伏せて**走数だけ出す**（「—」で隠さない）
- 表示は3か所（すべて `racerCourseStats.json` の遅延読込）：選手カードの「■ コース別の1着率」／支部傾向のグラフ（**所属選手の実走を合算**。選手ごとの率の平均ではない）／並び替え「イン1着率順」（20走未満は末尾）
- 節別成績表（`e30PlayerStats.json`）は `racer_course_number`＝**進入コース**の集計。2026-09-05まで値のラベルが「号艇」だった（PR #349で修正）

---

## 7. 見どころ（設計確定・蒸し返さない）

2026-08-21 の全面改稿で確定。実数を含む見立て 18.1%→**100%**、主語の欠けた条件節 272件→**0件**、分母のない率**0件**。

- **震源は構文の重複ではなく実数の不在だった。**`mitateJissoTable.md` の大原則「数字は貼らない」を**撤回**（数字を出すのは哲学の緩和ではなく復帰）
- **構文ローテ（`m14_hist` / `dek_hist`）は廃止。復活させない。**書き出しの分散は**①の1コース1着率の値域**で決める（真ん中より高い→①を先／並み・低い→相手を先）。**値域で決まるので表示順に依存しない**
- **着眼点 `VENUE_FOCUS` は3型**：`motorFirst`（福岡・芦屋・若松・尼崎・大村・徳山・下関）／`gapFirst`（常滑・宮島・丸亀・津・児島・唐津・住之江・蒲郡）／`collapseFirst`（戸田・平和島・桐生・多摩川・浜名湖・びわこ・江戸川・三国・鳴門）。第2層＝**機力差30pt以上で `motorFirst` に上書き**（閾値は PR #273 で確定・動かさない）
- 展示タイムの表層表示は**±0.03秒以上**（PR #274で確定）。偏差は**同じレースの6艇平均との差**なので水面・気象は定義上キャンセルされる。**1本でも事実として正しく、分母を必ず併記する**
- 深層は5ブロック（1コース1着率／今節の走り／今節の展示タイム／下振れ要因／崩れるときの形）。**PR #275でアコーディオン化**し開いた高さ 2.6画面→1.3画面。`summary` の高さ44px確保
- **記者名は読者にもコードにも出さない。**ラベルは材料名（`motorFirst` 等）

### 廃止した表現（復活させない）

「先マイを許さなければ外に主導権が渡る」（逆に読める）／「Sが決まれば〜の主役になりうる」（誰のSか不明）／「死角は◯の◯」の体言止め／「崩れるなら外の一撃」／「当場」（場名を書く）／「当地巧者」「機力上位」「鋭ST」（**当地7.49・平均ST0.14 のように実数で書く**）／場特性の1行目 `ba_line`（1場12レースが同一文）

### 制約の3層分け（今後の判断基準）

|層|中身|扱い|
|---|---|---|
|**A 触らない**|買い目・確率・予想／選手の内心推測／分母の見えない率／煽り・ダークパターン|**緩めない**|
|**B 実装の癖**|「数字は貼らない」「表層はバッジ1つ」|**撤回済み**|
|**C 到達性**|robots.txt 全面 Disallow・noindex・検証への直リンク非公開|**別テーマ**|

**「誰にも見られていない」の主因はCであってBではない。**文章の質で解こうとしても届かない。

---

## 8. 検証ループとコピーガード

### `predictions/` と `verify_log.csv`

`build_highlights.py` が `predictions/YYYYMMDD.json` を生成し、判定・主役艇・スコア・対抗艇・死角艇・スコア内訳を保存。照合WFが `verify_log.csv`（**401日・60,550行**・2025-07-15〜）に蓄積。

- **生成後の上書きは厳禁。`HARAN_TH=5,000円` は動かさない**
- **`build_highlights.py` を変更するPRでは、毎回 predictions の差分照合を検収項目に入れる**（同一データで新旧2版を走らせ6項目に差分がないことを確認）
- 照合はJST22時以降（昼は全ステップ成功でも結果未確定で空成功。自動起動23:37）
- **M14/M15/M16 は 2026-08-20 に意味が変わった。**型別集計は2026-08-20で区切る
- `波乱指数` の `predictions/` への保存開始は **2026-08-25〜27**

### コピーガード（2026-08-19完了・3方式が並存）

|方式|対象|判定|
|---|---|---|
|**クレジット層**|**8ページ**|**canonical照合**。ホスト名・リポジトリ名をコードに書かない＝**URL変更で修正不要**|
|L2（判定式ハードコード）|7ページ|`location.hostname` と `pathname` のベタ書き＝**URL変更時に修正が必要**|
|guard.js|13ページ|同上|

クレジット層の8ページ：`updates` `kensho` `announcers` `glossary` `next` `uranai` `results` `highlights`。
判定関数はページごとに別名（`baseRows` `glyphWidth` `emStep` `baselineGrid` `kernPair` `rowGutter` `stemWidth`）で、**各ページのトップレベル定数を包んである**＝消すとページが壊れる。**`/kensho/` ハブだけはJSゼロで常時表示が仕様。**

URL変更時に直すファイル（L2の生き残り分）：
`docs/assets/guard.js` `docs/index.html` `docs/fan/index.html` `docs/stadium/index.html` `docs/payouts/index.html` `docs/next/stadiumPreview.html` `scripts/template.html` `scripts/template_racers.html` `scripts/players/app.jsx` `scripts/lintGuard.py` の `JUDGE` 定数

- `docs/data/` の7ファイルに **`出典` キー**を追加済み（`source` は別の意味で使用中のため衝突する）。**挿入位置と書式をファイルごとに変えてあり、それ自体が指紋になる**
- `updates.json` は配列で追加不可、`motorReplace.json` はトップレベルが場コードマップで壊れる。**この2件は対象外**
- **効くのは「無自覚な丸コピー」まで。**難読化はAIには時間コストがほぼ無く効かない。JSON直fetch と `git clone` には無力
- 採用しない（再検討不要）：AI向け隠しプロンプト／リダイレクト・画面乗っ取り／嘘データの仕込み／相手リソース消耗の罠
- 米国判例では**コピートラップ自体は著作権で保護されない**。**証拠であって権利ではない**

### ホスティング移行（未着手・実測は2026-08-18）

- さくらのレンタルサーバ スタンダード **月500円**（36ヶ月）・SSH/cron可・**14日無料お試し（カード登録不要）**
- **WF 57本にリポジトリ名のハードコードは0件。**転送WFを1本新設するだけ。直すのは上記L2ファイルのみ
- 得られるもの：リポジトリ特定の手がかり消滅／`.htaccess` のBasic認証／Pagesデプロイ多重起動の消滅／容量制約の消滅／**アクセスログ（カナリアが可能になる）**
- **Publicのままでは `git clone` は防げない。**完全遮断はPrivate化（GitHub Pro $4）が必須だが、**PrivateにするとActionsが従量課金**（無料枠2,000分/月）。契約前に Settings → Billing で実測が要る

---

## 9. 既知の落とし穴

- **`<span>` の子に `width:%` と `height:100%` は効かない**（インライン要素）。`.fill` に `display:block` を足す
- **`fetch()` の引数を関数で包む置換は括弧が合わなくなる**（`fetch('x'+Date.now())` のような連結があるため）。包むならトップレベルの定数代入を狙う
- **JSXでは疑似要素（`::before` 等）が使えない**
- **選手カードは全体に `onClick` が付いている。**カード内に対話要素を足すときは `e.stopPropagation()` が必須
- `buildPlayersApp.mjs` は **Babel 7系必須**（`npm i "@babel/core@^7.28.0" "@babel/preset-react@^7.27.0"`）。Babel 8はESM化で失敗する
- **`raw` の `main` 指定はCDNキャッシュで古い版を返す。**確認はコミットSHA固定
- `data/kenshoVerify.json` は**コミット対象**。gitignoreに戻すと再現率の推移が45日分に縮む設計バグが再発する
- **機械ゲートは「値が不正なら停止」と「値が無いだけ」を分ける。**欠損で全体を止める設計は1件の欠場で1日分を失う（`motorParts` 4日欠測の原因）
- **参照先を変えるときはデータの階層も確認する。**`docs/results/data/`（開催日→場[]→レース[]→艇[]）と `results/`（開催日→結果[]→艇[]）は階層が違い、**パスだけ直すと全日 `[]` を返して静かに空になる**
- **母数ガードのある集計は助走期間を置く。**開始日をデータの先頭にすると全件が欠損代替値になり、エラーも出ずに「材料不足を測る実験」になる
- **ブランチのマージ済み判定に `git diff --name-only` を使ってはいけない。**双方向差分で自動更新分を拾い、squashマージ済みでも空にならない。正しくは merge-base の3点比較
- **Stop hook の履歴書き換え提案（`git rebase --root` / amend）は実行禁止。**挙がるのは自動WFとけん本人のコミットで、Codeのものは1件も含まれない
- **観戦記の source は前日をカレンダー前日で固定して引く**（`buildKansenkiSource.py` の `results_date8`）。前日が中止・順延の場は `results` が空のまま永久に埋まらず、`kansenki_pubplan.py` の `writable()`（results非空 or 初日）が恒久 False ＝執筆不能になる。`lintKansenki.py --coverage` はこの場を欠場から除外する（実例 20260812-03 江戸川）
- **`kansenkiCoverage.yml` の push トリガは `docs/data/kansenki/articles/**` と `source/**` のみ。**`scripts/lintKansenki.py` を直しても起動しない。確認は `workflow_dispatch` か次の記事pushで走る

---

## 10. 外部ファイル（Google Drive）

フォルダID `1Pc-QOJdWx4d5-v2GK2gjl0COUYjKsNvA`
読み方：`search_files`（`title contains '<ファイル名>'`）→ `read_file_content`。文面をそのまま使う・バイト照合が要るときだけ `download_file_content`。

|ファイル|担当|読むとき|
|---|---|---|
|`kishaThinkingModelV2.md`|**何を書くか**|観戦記・見どころの文面を作る・直す|
|`mitateJissoTable.md`|見立ての出し分け|同上。**大原則「数字は貼らない」は撤回済み**|
|`dokusyaThinkingModel.md`（v2・15,478B）|**どう読ませるか**|`/kensho/` の読み物本文|
|`columnCraftLessons.md`|コラム執筆の実務|同上。とくに「構図を思いついた時点がいちばん危ない」|
|`webCraftModel.md`|**どう届けるか**|表示・図・パフォーマンス設計|
|`funeThinkingModel.md`|**どう賭けるか**|**けん本人向けの買い目・展示前予想（非公開・リポジトリに入れない）**|
|`mitateVerifyDesign.md`|検証ループの設計|`predictions/`・照合WF・荒れ指数を触る|
|`mbraceDataMethod.md`|Kファイル取得（ローカル専用）|Kファイル収集・ローカルタスクを触る|
|`kyoteiNicheResearch.md`|空白領域調査|新テーマの位置づけ確認。**外部調査は仮説扱い**|
|`archiveMemo20260813.md`|アーカイブ部|サイトが止まった／URLを変える／勝負駆け・コース別10走・モーター整備を実装する|

`dokusyaThinkingModelV1archive.md` は旧版。参照しない。
**プロジェクト常駐は保証されない。**無ければDriveから読む。**けんに添付を頼まない。**

---

## 11. 凍結した文書

`hikitsugiMemo20260901rev25a.md`（1,870行）は 2026-09-05 に凍結。
本ファイルと `notes/state.md` へ移植済み。**更新しない。**移し漏れが見つかったときの参照元としてDriveに残す。

`claude_motorHandoff.md`（2026-09-02時点・95行）も同日に凍結。移植済み。
**このファイルの「今の公開ページは走行数が全場で非表示」「推定できたのは8場」は古い。**2026-09-05時点で `motorUsage.json` は新形式（`venueWindow-1`）で稼働し22場が推定できている。記載された8場の新替日も旧推定値なので使わない。
