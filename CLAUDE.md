# CLAUDE.md — boatrace プロジェクト 行動規範

このファイルは**行動規範だけ**を置く。仕様・構成・データ範囲・既知の問題などの
辞書的な情報は **`REFERENCE.md`** にある。必要になったときにその節だけ読む。

**異常が「見えない」のがこのリポジトリの主要な故障モード。緑や無言を正常と解釈しないこと**
（→ `REFERENCE.md` (1)(8)）。

## 応対方針
- **常に日本語のみで返答する。**
- **思考過程（reasoning / 内部検討）は表示しない。** 結論と必要な説明だけを簡潔に返す。
- 判断の主体は**けん**、操作の主体は**Code**。

---

## 承認なしでやっていいこと
確認画面を出さずに実行してよい。いちいち聞かない。

- bash コマンドの実行、ファイルの読み書き・編集
- git の add / commit / branch / push（**作業ブランチへの push のみ**）
- リポジトリ内の調査（grep・ファイル閲覧・スクリプトの試走）
- 公開データに影響しないローカル生成物の作り直し・確認
- PR の作成、PR 本文の更新
- **観戦記記事（`docs/data/kansenki/articles/`）の追加。**
  ただし `lintKansenki.py` が PASS したものだけ。**既存記事は上書きしない。**
  push 先は作業ブランチ。main への反映は従来どおり承認を要する。
  - **「main 直行でよい」のは自動タスク（`writeKansenkiLocal.ps1`）の挙動**であって、
    Code が手で書く場合の権限ではない。書き手には
    `docs/data/kansenki/runbook.md` の禁止事項（PR作成・マージ・push）が優先される。
    **混同しないこと。**

## 承認が要ること
実行前にけんへ確認する。

- **マージ**（merge / squash / rebase いずれも）
- **`main` への直接 push**
- 既存の運用フロー・スケジュールの変更（ワークフローの cron 追加・削除、タスク登録の変更）
- **`REFERENCE.md` (6) の定数**を変える必要が出たとき（値の根拠ごと提示して確認する）
- 公開ページの構成・見せ方を変える判断（表示文言の方針、ページの追加・削除）
- 外部サービスへの送信を伴う操作（LINE 送信、Issue 起票などを手動で行う場合）

## 絶対禁止
承認を取っても、原則としてやらない。やる必要が生じたら**まず相談する**。

- **裁定を経ないマージ。** 内容が正しいことは、マージしてよい理由にならない。
- **`predictions/YYYYMMDD.json` の上書き**（write-once が検証ループの根幹 → `REFERENCE.md` (5)）。
  `regenPredictions.py` / `regenPredictions.yml` は**原則使用禁止**。
- **`verify_log.csv` / `predictions_gaps.csv` の欠番の埋め戻し。** 欠番は欠番のまま残すのが正しい状態。
- **`REFERENCE.md` (6) の定数を1箇所だけ変えること。** `HARAN_TH` は3ファイルに重複定義があり、
  片方だけ動かすとエラーも警告も出ないまま検証の数字が食い違う。
- **生成物の直接編集**（`docs/motor/index.html` / `docs/racers/index.html` / `docs/players/app.js` ほか）。
  必ずテンプレート側を直して再生成する（対応表は `REFERENCE.md`「生成物とその編集元」）。
- **`kdata/` への書き込み**（保全用・読み取り専用）。`data/kfiles/` の lzh をコミットすること。
- **公開物に買い目・確率・勝者の断定・選手の内心推測を出すこと。**
  判定や主役艇などの内部値を公開 `highlights.json` に入れること。
- **認証情報・個人情報・非公開メモを追跡ファイルに書くこと**（このリポジトリは public）。
- **データの推測での補完。** 取得できなかった日は欠けたままにする。
  分からないことは `REFERENCE.md`「未確認事項」に、分からないまま書く。

---

## 作業の型（Phase制）

  Phase 1 調査 → Phase 2 実装 → Phase 3 検証 → Phase 4 PR作成・報告 → 〔けんの裁定〕→ Phase 5 マージ

**既定は Phase 4 で停止する。** PR は open のまま残す。
けんの承認（「OK」「マージして」等）が出てはじめて Phase 5 に進む。
マージ操作は Code が実行する。けんに GitHub 上での操作をさせない。

| Phase | やること | 出すもの |
|---|---|---|
| 1 調査 | 影響範囲を掴む。既存の型（似た実装）を先に探す | 分かったこと・方針を短く |
| 2 実装 | 作業ブランチで実装。既存コードの書き方に合わせる | — |
| 3 検証 | 実際に動かして確認する。動かせないなら「動かせない」と言う | 確認できたこと／できなかったこと |
| 4 PR作成・報告 | commit → push → PR 作成 → 報告して**止まる** | PR リンク・変更点・懸念 |
| 5 マージ | 承認が出てから実行 | マージ完了の報告 |

- Phase をまたぐときは、いま何をしたか一言添える。長い実況はしない。
- 途中で方針が変わる判断が必要になったら、そこで止めて聞く。勝手に広げない。
- **複数セッションが同じ作業を並走する場合、作業開始前に必ず `origin/main` へ fetch して
  現物を確認すること。古い作業ツリーの状態を根拠に「未着手」と判断してはならない。**
  2026-08-02、20260801 の観戦記で13本を上書きしかける事例が発生した。
  `assign_styles.py` の `locked=true` / `kansenki_pubplan.py` の `done` の確認を怠らないこと。

### 報告の作法
- **結論を先に書く。** 何をしたか → 確認できたこと → 残った懸念、の順。
- **できなかったことを省かない。** 検証していないなら「未検証」と明記する。
  テストが落ちているなら落ちていると書く。
- 数字や日付を出すときは**実測値**を出す。推測なら推測と書く。
- 長さは用件に見合わせる。作業が小さいなら報告も数行でよい。
- **報告は1つのコードブロックにまとめて出す。** けんがそのままコピペできる形にする。
  地の文と報告を混ぜない。複数の話題があっても分割せず、1ブロックに収める。

---

## PR のルール
- 読者に見える変更（ページの表示・機能・データが変わるもの）を含む PR は、
  本文に次の1行を必ず入れる。

      [updates] ページ名 / 何をしたか（読者向けの平易な一文）

  - `updateUpdates.yml` がこの行を `docs/data/updates.json` に記録し、`/updates/` に表示する。
  - ページ名に「/」は使えない（本文との区切りに使うため）。
  - 変更が複数あれば行を複数書いてよい。
- **リポジトリ内部だけの変更には入れない**（リファクタ・CI調整・ドキュメント追加など、
  読者に見えないもの）。
