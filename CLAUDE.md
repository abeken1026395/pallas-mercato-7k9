# CLAUDE.md — boatrace プロジェクト 行動規範

このファイルは**行動規範だけ**を置く。仕様・データ範囲・既知の問題などの辞書と、
**規範の根拠になった事例（(10)）は `REFERENCE.md`**、
**数字・文章・画面の作法は `METHOD.md`** にある。必要な節だけ読む。

**異常が「見えない」のがこのリポジトリの主要な故障モード。緑や無言を正常と解釈しないこと**（→ (1)(8)）。

## 応対方針
- **常に日本語のみで返答する。**
- **思考過程（reasoning / 内部検討）は表示しない。** 結論と必要な説明だけを簡潔に返す。
- 判断の主体は**けん**、操作の主体は**Code**。

---

## 承認なしでやっていいこと
確認画面を出さずに実行してよい。いちいち聞かない。

- bash コマンドの実行、ファイルの読み書き・編集
- git の add / commit / branch / push（**作業ブランチへの push のみ**）
- リポジトリ内の調査（grep・ファイル閲覧・スクリプトの試走）
- 公開データに影響しないローカル生成物の作り直し・確認
- PR の作成、PR 本文の更新
- **観戦記記事（`docs/data/kansenki/articles/`）の追加。** `lintKansenki.py` PASS 分のみ・
  **既存は上書きしない**・push 先は作業ブランチ（「main 直行」は自動タスクの挙動
  であって Code の権限ではない → (10-a)）。

## 承認が要ること
実行前にけんへ確認する。

- **マージ**（merge / squash / rebase いずれも）
- **`main` への直接 push**
- 既存の運用フロー・スケジュールの変更（ワークフローの cron 追加・削除、タスク登録の変更）
- **`REFERENCE.md` (6) の定数**を変える必要が出たとき（値の根拠ごと提示して確認する）
- 公開ページの構成・見せ方を変える判断（表示文言の方針、ページの追加・削除）
- 外部サービスへの送信を伴う操作（LINE 送信、Issue 起票などを手動で行う場合）

## 絶対禁止
承認を取っても、原則としてやらない。やる必要が生じたら**まず相談する**。

- **裁定を経ないマージ。** 内容が正しいことは、マージしてよい理由にならない。
- **`predictions/YYYYMMDD.json` の上書き**（write-once が検証ループの根幹 → (5)）。
  `regenPredictions.py` / `regenPredictions.yml` は**原則使用禁止**。
- **`verify_log.csv` / `predictions_gaps.csv` の欠番の埋め戻し。** 欠番のまま残すのが正しい状態。
- **`REFERENCE.md` (6) の定数を1箇所だけ変えること。** `HARAN_TH` は3ファイル重複定義（→ (6)）。
- **生成物の直接編集。** 必ずテンプレート側を直して再生成する（対応表 →「生成物とその編集元」）。
- **既存の観戦記記事の上書き・削除**（公開後の記事は不変が鉄則 → `runbook.md`）。
- **`kdata/` への書き込み**（保全用・読み取り専用）。`data/kfiles/` の lzh をコミットすること。
- **公開物に買い目・確率・断定・内心推測を出すこと。** 判定・主役艇などの内部値を公開 `highlights.json` に入れること。
- **認証情報・個人情報・非公開メモを追跡ファイルに書くこと**（このリポジトリは public）。
- **データの推測での補完。** 取得できなかった日は欠けたまま。分からないことは「未確認事項」に、分からないまま書く。

---

## 作業の型（Phase制）

  Phase 1 調査 → Phase 2 実装 → Phase 3 検証 → Phase 4 PR作成・報告 → 〔けんの裁定〕→ Phase 5 マージ

**既定は Phase 4 で停止する。** PR は open のまま残す。けんの承認（「OK」「マージして」等）が
出てはじめて Phase 5 に進む。マージ操作は Code が実行し、けんに GitHub 上での操作をさせない。

| Phase | やること | 出すもの |
|---|---|---|
| 1 調査 | 影響範囲を掴む。既存の型（似た実装）を先に探す | 分かったこと・方針を短く |
| 2 実装 | 作業ブランチで実装。既存コードの書き方に合わせる | — |
| 3 検証 | 実際に動かして確認する。動かせないなら「動かせない」と言う | 確認できたこと／できなかったこと |
| 4 PR作成・報告 | commit → push → PR 作成 → 報告して**止まる** | PR リンク・変更点・懸念 |
| 5 マージ | 承認が出てから実行 | マージ完了の報告 |

- Phase をまたぐときは一言添える。長い実況はしない。途中で方針が変わる判断が必要になったら、
  そこで止めて聞く。勝手に広げない。
- **作業開始前に必ず `origin/main` へ fetch して現物を確認する。** 古い作業ツリーの状態を
  根拠に「未着手」と判断してはならない（→ (10-b)）。

### 報告の作法
- **すべての報告の冒頭に、次のヘッダを必ず書く。** 値はすべて実測で、
  **報告を書く直前に `git fetch origin` を実行してから取る**（→ (10-b)）。

      ブランチ : git branch --show-current
      HEAD     : git rev-parse --short HEAD
      origin/main との差 : 何コミット先行／遅れているか
      作業内容 : 1行

  - アプリ側のセッションタイトルは Code から参照できない場合がある。
    取得できるなら書く。取得できなければ**ブランチ名で代替**する。
- **結論を先に書く。** 何をしたか → 確認できたこと → 残った懸念、の順。
- **できなかったことを省かない。** 未検証なら「未検証」、テストが落ちているなら落ちていると書く。
- 数字や日付は**実測値**を出す（推測なら推測と書く）。長さは用件に見合わせる。
- **報告は1つのコードブロックにまとめて出す。** けんがそのままコピペできる形にする。
  地の文と報告を混ぜない。複数の話題があっても分割しない。**表や図もブロック内に収める。**

---

## PR のルール
- 読者に見える変更（ページの表示・機能・データが変わるもの）を含む PR は、本文に次の1行を
  必ず入れる。`updateUpdates.yml` がこれを拾い `/updates/` に記録する。

      [updates] ページ名 / 何をしたか（読者向けの平易な一文）

  ページ名に「/」は使えない（本文との区切りに使うため）。複数あれば行を複数書いてよい。
- **リポジトリ内部だけの変更には入れない**（リファクタ・CI調整・ドキュメント追加など）。
